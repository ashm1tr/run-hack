let allCourses=[],raf=0,gpsWatch=null;fetch('../data/courses.json').then(r=>r.json()).then(x=>allCourses=x);const ui=()=>{if(document.querySelector('#race-mode'))return document.querySelector('#race-mode');const el=document.createElement('div');el.id='race-mode';el.innerHTML='<div class="race-hud"><div class="eyebrow">LIVE RACE // <span id="race-mode-label">SIMULATOR</span></div><h2 id="race-name">Race</h2><div class="race-metrics"><div><small>SIMULATED CLOCK</small><strong id="race-time">00:00</strong></div><div><small>DISTANCE</small><strong id="race-distance">0.00 km</strong></div><div><small>LIVE PACE</small><strong id="race-pace">—</strong></div><div><small>UPCOMING ELEVATION</small><strong id="race-elevation">— m</strong></div></div><div class="race-controls"><button id="race-pause">Pause</button><button id="race-gps">Use GPS</button><label>TIME COMPRESSION <input id="race-speed" type="range" min="0.5" max="4" step="0.5" value="1.5"></label><button id="race-exit">Exit</button></div><div id="race-finish" hidden></div></div>';document.body.append(el);return el};const clock=s=>{const m=Math.floor(s/60),sec=Math.floor(s%60);return String(m).padStart(2,'0')+':'+String(sec).padStart(2,'0')};const gain=c=>c.elevation.reduce((n,v,i)=>n+(i&&v>c.elevation[i-1]?v-c.elevation[i-1]:0),0);function start(c){const el=ui();el.hidden=false;document.querySelector('#race-name').textContent=c.name;const targetPace=5.9+c.difficulty*.1+c.incline*.12,targetSeconds=c.distanceKm*targetPace*60;let last=performance.now(),simTime=0,distance=0,paused=false,splits=[],lastKm=0;const tick=now=>{if(!paused){const dt=(now-last)/1000,compression=Number(document.querySelector('#race-speed').value)/1.5;simTime+=dt*targetSeconds/17*compression;const gradient=(c.elevation[Math.min(c.elevation.length-1,Math.floor(distance/c.distanceKm*(c.elevation.length-1))+1)]-c.elevation[Math.min(c.elevation.length-1,Math.floor(distance/c.distanceKm*(c.elevation.length-1)))]||0);const pace=targetPace*(1+Math.max(-.12,Math.min(.22,gradient*.018)));distance=Math.min(c.distanceKm,distance+(dt*compression/60)/pace);while(lastKm+1<=Math.floor(distance)){lastKm++;splits.push({km:lastKm,time:simTime})}const i=Math.min(c.route.length-1,Math.floor(distance/c.distanceKm*(c.route.length-1)));document.querySelector('#race-time').textContent=clock(simTime);document.querySelector('#race-distance').textContent=distance.toFixed(2)+' km';document.querySelector('#race-pace').textContent=pace.toFixed(2)+' min/km';document.querySelector('#race-elevation').textContent=(c.elevation[Math.min(i+1,c.elevation.length-1)]||0)+' m';if(window.__runMap)window.__runMap.flyTo({center:c.route[i],zoom:15,pitch:55,duration:250});if(distance>=c.distanceKm)return finish(c,simTime,splits)}last=now;raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);document.querySelector('#race-pause').onclick=()=>{paused=!paused;document.querySelector('#race-pause').textContent=paused?'Resume':'Pause'};document.querySelector('#race-exit').onclick=()=>{cancelAnimationFrame(raf);if(gpsWatch)navigator.geolocation.clearWatch(gpsWatch);el.hidden=true};document.querySelector('#race-gps').onclick=()=>{if(!navigator.geolocation)return;document.querySelector('#race-mode-label').textContent='GPS';cancelAnimationFrame(raf);gpsWatch=navigator.geolocation.watchPosition(p=>{const here=[p.coords.longitude,p.coords.latitude];if(window.__runMap)window.__runMap.flyTo({center:here,zoom:17,pitch:60,duration:600});if(p.coords.speed)document.querySelector('#race-pace').textContent=((p.coords.speed*3.6)**-1*16.667).toFixed(2)+' min/km'})}}function finish(c,seconds,splits){const key='run-league-pb:'+c.id,old=Number(localStorage.getItem(key)||0),pb=!old||seconds<old;localStorage.setItem(key,String(pb?seconds:old));const splitText=splits.map(s=>'KM '+s.km+' '+clock(s.time)).join(' · ');const box=document.querySelector('#race-finish');box.hidden=false;box.innerHTML='<h3>'+ (pb?'NEW PERSONAL BEST':'FINISH LINE')+'</h3><p>Time: '+clock(seconds)+'</p><p>Splits: '+(splitText||'Finish split recorded')+'</p><p>Cumulative elevation: '+gain(c)+' m climbed</p><button onclick="this.closest(\'#race-mode\').hidden=true">Back to course</button>}document.querySelector('#start-race').addEventListener('click',()=>{const id=location.hash.startsWith('#course/')&&location.hash.slice(8),c=allCourses.find(x=>x.id===id)||allCourses[0];if(c)start(c)});