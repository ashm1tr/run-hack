const KEY='run-league-state';const state=JSON.parse(localStorage.getItem(KEY)||'{"xp":0,"streak":0,"badges":[]}');const seeded={"regents-park-loop":[['Maya',1740],['Theo',1812],['Ava',1935]],"hyde-park-loop":[['Theo',2080],['Maya',2165],['Jordan',2290]],"richmond-park-tamsin":[['Maya',4020],['Theo',4310],['Jordan',4580]]};function save(){localStorage.setItem(KEY,JSON.stringify(state))}function course(){const id=location.hash.startsWith('#course/')&&location.hash.slice(8);return id}function challenge(){const id=course()||'regents-park-loop',ghost=Number(new URLSearchParams(location.search).get('ghost'))||2100;history.replaceState({},'',location.pathname+'?course='+encodeURIComponent(id)+'&ghost='+ghost+'#course/'+id);navigator.clipboard?.writeText(location.href);const n=document.createElement('div');n.className='challenge-toast';n.textContent='Challenge link copied — your rival is ready';document.body.append(n);setTimeout(()=>n.remove(),3000)}function versus(){const id=new URLSearchParams(location.search).get('course');if(!id)return;const ghost=Number(new URLSearchParams(location.search).get('ghost'))||2100;const box=document.createElement('div');box.className='versus-card';box.innerHTML='<div class="eyebrow">HEAD TO HEAD</div><h2>YOU <span>VS</span> RIVAL GHOST</h2><div class="versus-track"><i id="you-dot">●</i><b id="gap">+0.0s</b><i id="rival-dot">●</i></div><p id="versus-result">Runners ready.</p>';document.body.append(box);let t=0;const go=()=>{t+=.1;const you=t/(ghost/17),rival=Math.min(1,t/17),gap=(you-rival)*ghost;box.querySelector('#you-dot').style.left=Math.min(95,you*95)+'%';box.querySelector('#rival-dot').style.left=Math.min(95,rival*95)+'%';box.querySelector('#gap').textContent=(gap>0?'+':'')+gap.toFixed(1)+'s';if(t<17)requestAnimationFrame(go);else{box.querySelector('#versus-result').textContent=you>=rival?'PHOTO FINISH — YOU WIN':'PHOTO FINISH — RIVAL WINS';state.xp+=(id.length*3);state.streak++;if(!state.badges.includes('Rival Beaten')&&you>=rival)state.badges.push('Rival Beaten');save()}};go()}document.addEventListener('click',e=>{if(e.target.closest('#detail-invite')){e.preventDefault();challenge()}});if(new URLSearchParams(location.search).has('ghost'))setTimeout(versus,700);