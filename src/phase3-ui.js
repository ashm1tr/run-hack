// Phase 3 UI enhancement module: leaderboard, progression, and map runner presentation.
const progressionKey='run-league-state';const seeded={"regents-park-loop":[['Maya',1740],['Theo',1812],['Ava',1935]],"hyde-park-loop":[['Theo',2080],['Maya',2165],['Jordan',2290]],"richmond-park-tamsin":[['Maya',4020],['Theo',4310],['Jordan',4580]]};function renderProgression(){let s=JSON.parse(localStorage.getItem(progressionKey)||'{"xp":0,"streak":0,"badges":[]}'),level=Math.floor(s.xp/500)+1,pct=Math.min(100,s.xp%500/5);let el=document.querySelector('#progression-widget');if(!el){el=document.createElement('aside');el.id='progression-widget';el.className='progression-widget';document.body.append(el)}el.innerHTML=`<div class="eyebrow">RUNNER PROFILE</div><strong>LEVEL ${level}</strong><div class="xp-track"><i style="width:${pct}%"></i></div><small>${s.xp%500}/500 XP · ${s.streak} day streak</small><div class="badge-row">${(s.badges||[]).map(b=>`<span>${b}</span>`).join('')||'<span>FIRST RUN</span>'}</div>`}function renderLeaderboard(c){let card=document.querySelector('.course-card.selected');if(!card)return;let rows=(seeded[c.id]||[['Maya',Math.round(c.distanceKm*360)],['Theo',Math.round(c.distanceKm*390)],['Jordan',Math.round(c.distanceKm*420)]]).slice().sort((a,b)=>a[1]-b[1]);let el=card.querySelector('.leaderboard');if(!el){el=document.createElement('div');el.className='leaderboard';card.append(el)}el.innerHTML='<b>LOCAL LEADERBOARD</b>'+rows.map((r,i)=>`<div><span>${['🥇','🥈','🥉'][i]||'·'} ${r[0]}</span><strong>${Math.floor(r[1]/60)}:${String(r[1]%60).padStart(2,'0')}</strong></div>`).join('')}renderProgression();window.addEventListener('hashchange',renderProgression);